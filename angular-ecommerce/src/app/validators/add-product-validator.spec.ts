import { AddProductValidator } from './add-product-validator';

describe('AddProductValidator', () => {
  it('should create an instance', () => {
    expect(new AddProductValidator()).toBeTruthy();
  });
});
